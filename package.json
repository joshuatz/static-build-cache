{
	"name": "glitch-static-cache",
	"version": "1.0.0",
	"description": "Cache your static builds on Glitch to avoid startup times",
	"main": "src/index.js",
	"bin": {
		"glitch-cache": "echo '@todo'"
	},
	"repository": {
		"type": "git",
		"url": "@TODO"
	},
	"license": "MIT",
	"author": "Joshua Tzucker",
	"homepage": "@TODO",
	"keywords": [
		"glitch cache",
		"glitch startup",
		"glitch.com"
	],
	"scripts": {
		"build": "echo '@todo'",
		"format": "yarn prettier --check src",
		"test": "echo 'no tests yet :('",
		"scratch": "SET GLITCH_SHARED_INCLUDES_LEGACY_CLS=\"true\" && ts-node --transpile-only src/cli.ts && echo 'exited!'"
	},
	"dependencies": {
		"commander": "^6.0.0",
		"detect-is-on-glitch": "^1.0.0",
		"serve": "^11.3.2"
	},
	"devDependencies": {
		"@types/node": "^14.0.23",
		"prettier": "2.0.5",
		"shx": "^0.3.2",
		"type-fest": "^0.16.0"
	}
}
